/** Preset spending categories for a bank withdrawal/spend — feeds the spending-by-category graphs.
 *  Free text is still accepted (an "Other" pick shows a text input) so this isn't a hard limit. */
export const SPENDING_CATEGORIES = ['Food', 'Toys', 'Clothes', 'Entertainment', 'Books & Games', 'Gifts', 'Savings', 'Other'];
